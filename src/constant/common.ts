export enum RoleEnum {
  ADMIN = 'ADMIN',
  HOST = 'HOST',
  CUSTOMER = 'CUSTOMER',
}

export enum StatusEnum {
  ACTIVE = 1,
  INACTIVE = 0,
  DELETED = 'DELETED',
}

export enum BookingEnum {
  PENDING = 'PENDING',
  CANCEL = 'CANCEL',
  CHECKED = 'CHECKED',
}
